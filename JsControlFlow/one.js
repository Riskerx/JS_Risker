//if

const firstdata = "10";
const seconddata = 10;
function checkDataType(a, b) {
    if (a === b) {
        console.log("data type is same");
    }
    else {
        console.log("data type is not same");
    }
}
checkDataType(firstdata, seconddata); // Outputs: data type is same

